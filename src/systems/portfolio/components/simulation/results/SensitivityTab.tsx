import React from 'react';

interface SensitivityTabProps {
  results: any;
}

const SensitivityTab: React.FC<SensitivityTabProps> = ({ results }) => {
  return (
    <div style={{ padding: '1rem' }}>
      <p>Sensitivity tab is under reconstruction. Please use the Fund Overview tab for now.</p>
    </div>
  );
};

export default SensitivityTab;
